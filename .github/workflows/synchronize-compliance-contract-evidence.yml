name: synchronize-compliance-contract-evidence

on:
  push:
    branches:
      - "release/moneybee-repository-complete-20260902"
    paths-ignore:
      - "docs/API_ENDPOINT_CATALOG.md"
      - "docs/openapi/compliance-operator-api-manifest.json"

permissions:
  contents: write

concurrency:
  group: synchronize-compliance-contract-evidence-${{ github.ref }}
  cancel-in-progress: true

jobs:
  synchronize:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    env:
      APP_ENV: test
      DATABASE_URL: sqlite+aiosqlite:///./compliance-contract.db
      AUTO_CREATE_SCHEMA: "true"
      LOCAL_AUTH_BYPASS: "true"
      ENABLE_EXTERNAL_DELIVERY: "false"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"
          cache: pip
      - name: Install reviewed dependencies
        run: |
          python -m pip install --upgrade pip wheel setuptools
          python -m pip install -e ".[dev]"
          python -m pip check
      - name: Validate compliance behavior and generate deterministic evidence
        run: |
          python -m compileall -q app tests scripts
          python -m ruff check app tests scripts
          python -m pytest -q tests/test_compliance_routes.py tests/test_compliance_schema_strictness.py
          python scripts/sync_compliance_operator_manifest.py
          python scripts/generate_endpoint_catalog.py
          python scripts/verify_openapi_contract.py
      - name: Commit only generated compliance evidence
        env:
          BRANCH_NAME: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          allowed='^( M|M |A |AM|MM|\?\?) (docs/API_ENDPOINT_CATALOG\.md|docs/openapi/compliance-operator-api-manifest\.json)$'
          unexpected="$(git status --porcelain | grep -Ev "$allowed" || true)"
          if test -n "$unexpected"; then
            echo "Refusing to commit changes outside deterministic compliance evidence:" >&2
            printf '%s\n' "$unexpected" >&2
            exit 1
          fi
          if git diff --quiet -- docs/API_ENDPOINT_CATALOG.md docs/openapi/compliance-operator-api-manifest.json \
            && test -z "$(git status --porcelain -- docs/openapi/compliance-operator-api-manifest.json)"; then
            echo "Compliance evidence is already synchronized."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/API_ENDPOINT_CATALOG.md docs/openapi/compliance-operator-api-manifest.json
          git commit -m "docs(api): synchronize compliance contract evidence"
          git push origin "HEAD:${BRANCH_NAME}"
